# 灵感大王 · 代码体检报告（2026-07-15）

> **体检范围**：`D:\AI工作平台\灵感大王`（最新提交 `2088b3c`，v0.4.0 / M12 完成态）
> **体检方式**：只读扫描（Glob/Grep/Read），未运行 build/test，避免耗时
> **核心结论**：工程质量上乘，**安全设计堪称典范**；主要短板在测试覆盖与 lint 工程化

---

## 0. 项目规模概览

| 维度 | 数据 |
|------|------|
| 前端源文件（src/） | **88** 个 .ts/.tsx |
| Rust 源文件（src-tauri/src/） | **3** 个（lib.rs 843 行、scanner.rs 433 行、main.rs 5 行） |
| 组件数 | 24 个 .tsx |
| Engine 模块 | 14 个 .ts |
| Stores | 10 个 |
| Hooks | 6 个 |
| 测试文件 | 12 个（167 用例） |
| Tauri 命令 | 20 个 |

**最大文件 Top 6**：
1. `src-tauri/src/lib.rs` — 843 行（命令集中）
2. `src/engine/QuestionEngine.ts` — 536 行
3. `src/components/RequirementSyncPanel.tsx` — 536 行
4. `src/engine/PromptGenerator.ts` — 517 行
5. `src/hooks/useFlow.ts` — 498 行
6. `src/engine/RequirementSyncer.ts` — 495 行（M11 新增）

---

## 1. 五维度体检详情

### 维度 1：代码质量（B 级）

| # | 问题 | 证据 | 严重度 | 建议 |
|---|------|------|--------|------|
| 1.1 | **19 个 .bak 备份文件**污染源码树 | `find . -name "*.bak*"`：ExpandedCard.tsx.bak、QuestionLoader.ts.bak.20260623_213623、tauri.conf.json.bak 等 | 中 | `git rm` 全部，加 .gitignore |
| 1.2 | **package.json 有 lint 脚本但 eslint 未装** | package.json:11 `"lint": "eslint . --ext ts,tsx..."`；node_modules/eslint 不存在；`.eslintrc` 不存在 | **高** | 装 eslint + 写 .eslintrc，或删 lint 脚本 |
| 1.3 | **命名风格不统一** | `评价词典.ts`（中文）、`seedRouter.ts`（camelCase）、`QuestionEngine.ts`（PascalCase）混用 | 中 | 统一 PascalCase 或 kebab-case |
| 1.4 | **6 个文件超 500 行** | lib.rs 843、QuestionEngine 536、RequirementSyncPanel 536、PromptGenerator 517、useFlow 498、RequirementSyncer 495 | 中 | lib.rs 按职责拆子模块；useFlow 拆子 hook |
| 1.5 | as any 仅 1 处（动态导入） | `src/lib/sqlite.ts:23` `const Database = (mod as any).Database` | 低 | 补 @tauri-apps/plugin-sql 类型声明 |
| 1.6 | tsconfig strict 模式全开 | `"strict": true, "noUnusedLocals": true, "noImplicitReturns": true` | ✅ 亮点 | 可加 `noUncheckedIndexedAccess` |
| 1.7 | 无 console.log 残留 | 65 处 console 全为 error/warn/info，带 `[模块名]` 前缀 | ✅ 亮点 | 保持现状 |
| 1.8 | 无 TODO/FIXME/HACK 代码债务 | 残留为 ProjectScanDashboard 样式映射 | ✅ 亮点 | — |

### 维度 2：功能完整性（B 级）

| # | 问题 | 证据 | 严重度 | 建议 |
|---|------|------|--------|------|
| 2.1 | **6 个 engine 模块无单测** | seedRouter（432 行）、RequirementSyncer（495 行）、ImagePromptExtractor、ScreenshotDiagnoser、AdjectiveClusterLoader、IntentTagCollector | **高** | 优先补 seedRouter + RequirementSyncer |
| 2.2 | seedRouter 3 个 export 死代码 | `detectNegation`、`topTagScores`、`buildDynamicClarification` 0 引用 | 中 | 删 或 标 reserved |
| 2.3 | `build-release.ps1` 硬编码个人绝对路径 | `$env:Path = "d:\puj\下载;"`、`Set-Location "d:\AI工作平台\灵感大王"` | 中 | 改环境变量 / 相对路径 |
| 2.4 | 10 个 stores 职责单一 | engineStore 81、appStore 35，无上帝 store | ✅ 亮点 | 保持现状 |
| 2.5 | 无占位代码 / 空函数体 | grep `not implemented` 0 命中 | ✅ 亮点 | — |
| 2.6 | 组件分层约束文档化 | AGENTS.md:47-51 明确 UI→hooks→engine→lib→tauri 单向依赖 | ✅ 亮点 | 保持现状 |

### 维度 3：性能与安全（A 级）★ 优秀

| # | 防护点 | 证据 | 评级 |
|---|--------|------|------|
| 3.1 | **API Key 存储** | keyring → Windows Credential Manager；浏览器降级 sessionStorage（关闭即清） | ✅ 优秀 |
| 3.2 | **SSRF 防护** | validateBaseUrl：协议白名单 + IPv4 私有/保留 + IPv4-mapped IPv6 + 十进制/十六进制/八进制归一化 + IPv6 回环/链路本地，测试覆盖 169.254.169.254/10.0.0.1/2130706433 | ✅ 优秀 |
| 3.3 | **路径遍历防护** | scan_project/full_scan_project/read_image_file 均 canonicalize + 白名单目录校验 | ✅ 优秀 |
| 3.4 | **XSS 攻击面** | 移除 SVG 支持；无 dangerouslySetInnerHTML/v-html/eval/new Function/innerHTML | ✅ 优秀 |
| 3.5 | **CSP 与 capabilities** | CSP default-src 'self'、object-src 'none'；capabilities 22 个 permission 显式列出无通配 | ✅ 优秀 |
| 3.6 | **内存增长有界** | analyticsStore MAX_RECORDS=500、contextStore MAX_RECENT_QA=20/MAX_PREFERENCES=30，slice 截断 | ✅ 优秀 |
| 3.7 | **资源清理** | setTimeout/clearTimeout 配对、AbortController 超时、事件监听器 removeEventListener | ✅ 优秀 |
| 3.8 | **硬编码密钥** | grep api_key/secret/token/password 0 命中 | ✅ 优秀 |
| 3.9 | **竞态保护** | contextStore opsChain 串行化 + useFlow startSeq+mountedRef 双重保护（P1-4/5 修复） | ✅ 优秀 |
| 3.10 | read_image_file 防御纵深 | 白名单 + 黑名单 + 敏感文件名 + 10MB 大小限制 + 格式限制 | ✅ 优秀 |

**安全设计总结**：keyring 存密钥 + SSRF 全向量防护 + 路径遍历 canonicalize + CSP 严格 + capabilities 最小权限 + 内存有界 + 竞态串行化——**已具备生产级安全基线**。

### 维度 4：架构与可维护性（A 级）

| # | 问题 | 证据 | 严重度 | 建议 |
|---|------|------|--------|------|
| 4.1 | **类型循环引用** seedRouter ↔ LLMIntentAnalyzer | 双方 `import type` 双向引用 | 中 | 提取共享类型到 types.ts |
| 4.2 | useFlow 498 行职责偏重 | 含 LLM 分析、上下文回读、偏好提取、动态追问、startSeq 取消 | 中 | 拆 useFlowStart/useFlowAnswer 子 hook |
| 4.3 | IntentTagCollector 依赖注入 | 构造函数注入 QuestionEngine + PromptGenerator，type-only import | ✅ 亮点 | 可作其他模块范本 |
| 4.4 | 错误处理覆盖全 | try/catch 密度高，错误信息中文带模块前缀，降级链路清晰 | ✅ 亮点 | 保持现状 |
| 4.5 | 依赖精简无冗余 | uuid 6 处、yaml 2 处、其余核心 | ✅ 亮点 | 保持现状 |
| 4.6 | 扩展性良好 | 新增 LLM 服务商 = 加 PROVIDER_PRESETS；新增问题 = 改 YAML；新增命令 = 加 #[tauri::command] | ✅ 亮点 | 保持现状 |

### 维度 5：文档与注释（B 级）

| # | 问题 | 证据 | 严重度 | 建议 |
|---|------|------|--------|------|
| 5.1 | **无 LICENSE 文件**（Cargo.toml 声明 MIT） | 根目录 ls 无 LICENSE | 中 | 补 MIT 全文 + copyright |
| 5.2 | 无 CONTRIBUTING.md | 根目录无 | 低 | 加贡献流程文档 |
| 5.3 | README.md 偏项目介绍 | 64 行无 Quick Start/开发命令 | 低 | 补充或指向 AGENTS.md |
| 5.4 | Rust 文档注释不均 | lib.rs 辅助函数有 ///，#[tauri::command] 多数无 rustdoc | 低 | 为每个 command 补 /// |
| 5.5 | JSDoc 覆盖率 60-70% | LLMAdapter/validateBaseUrl/contextStore 详注；seedRouter 多 export 无注 | 低 | 为 engine 全部 export 补 JSDoc；可配 eslint require-jsdoc |
| 5.6 | docs/ 文档丰富 | 20+ 文档覆盖 PRD/架构/计划/QA/隐私/风险 | ✅ 亮点 | 保持现状 |
| 5.7 | CHANGELOG.md 规范 | Keep a Changelog 格式，含验证结果 | ✅ 亮点 | 保持现状 |
| 5.8 | AGENTS.md 工程指南 | 4503 字节，含技术栈/目录/分层/命令 | ✅ 亮点 | 保持现状 |

---

## 2. 问题汇总与优先级

### 严重度统计

| 严重度 | 数量 | 占比 |
|--------|------|------|
| **高** | 2 | 11% |
| **中** | 7 | 37% |
| **低** | 10 | 53% |
| **合计** | **19** | 100% |

### 维度评级

| 维度 | 评级 | 高 | 中 | 低 |
|------|------|----|----|-----|
| 1. 代码质量 | B | 1 | 3 | 4 |
| 2. 功能完整性 | B | 1 | 3 | 4 |
| 3. 性能与安全 | **A** | 0 | 0 | 10 |
| 4. 架构与可维护性 | **A** | 0 | 2 | 6 |
| 5. 文档与注释 | B | 0 | 1 | 8 |

### 整改优先级清单

#### 🔴 P0（高严重度，1 周内修复）

| # | 任务 | 工作量 | 验收 |
|---|------|--------|------|
| P0-1 | **安装并配置 ESLint**（补齐 lint 工具链） | 0.5d | `npm run lint` 成功 |
| P0-2 | **补齐 6 个 engine 模块的单元测试**（seedRouter 路由核心 + RequirementSyncer M11 新功能优先） | 2-3d | vitest 全绿，新增 30+ 用例 |

#### 🟠 P1（中严重度，1 月内修复）

| # | 任务 | 工作量 |
|---|------|--------|
| P1-1 | 删除 19 个 .bak 文件并加 .gitignore | 0.1d |
| P1-2 | 统一命名风格（`评价词典.ts` → `EvaluationDict.ts`） | 0.1d |
| P1-3 | 拆分 lib.rs（commands/context.rs、commands/project.rs、commands/api_key.rs） | 0.5d |
| P1-4 | 拆 useFlow 为 useFlowStart / useFlowAnswer 子 hook | 0.5d |
| P1-5 | 删除 seedRouter 死代码（detectNegation 等 0 引用 export） | 0.1d |
| P1-6 | 清理 build-release.ps1 硬编码路径 | 0.1d |
| P1-7 | 打破 seedRouter ↔ LLMIntentAnalyzer 类型循环（提取到 types.ts） | 0.2d |
| P1-8 | 新增 LICENSE 文件（MIT 全文 + copyright） | 0.1d |

#### 🟡 P2（低严重度，择机优化）

| # | 任务 | 工作量 |
|---|------|--------|
| P2-1 | 补 CONTRIBUTING.md | 0.2d |
| P2-2 | README 补充 Quick Start + 开发命令 | 0.2d |
| P2-3 | 为 #[tauri::command] 补 rustdoc | 0.5d |
| P2-4 | 为 engine export 函数补 JSDoc（可配 eslint require-jsdoc 强制） | 0.5d |
| P2-5 | sqlite.ts 动态导入消除 as any（补类型声明） | 0.1d |
| P2-6 | 引入统一 logger 模块（按 release 模式自动降级） | 0.3d |
| P2-7 | tsconfig 增加 `noUncheckedIndexedAccess` | 0.1d |

---

## 3. 一句话总结

**灵感大王是一个工程质量上乘的 Tauri 2.0 桌面应用，安全设计堪称典范。** 主要短板在于：
1. **ESLint 工具链缺失**（lint 脚本存在但无依赖）
2. **6 个 engine 模块无单测**（覆盖盲区集中在 seedRouter 路由核心）
3. **19 个 .bak 备份文件**污染源码树

修复 P0 两项（ESLint + 测试补齐）即可达到生产级开源标准。

---

> 本报告由 Explore agent 自动化扫描生成（基于 Glob/Grep/Read），由 WorkBuddy 整合校对。
> 详细证据见 plan§七 末尾链接，扩展方案见 `docs/智能化升级方案.md`。
