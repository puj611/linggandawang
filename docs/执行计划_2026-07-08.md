# 灵感大王 · 可执行冲刺计划（07/08 → 07/22）

> 本文档基于 `docs/项目进度计划书.md`（v0.2.0，07/04）与实际代码交叉验证生成。
> 生成日期：2026-07-08（周三）。初赛截止 07/15（剩 7 天），大赛冻结 07/22（剩 14 天）。

---

## 0. 执行总原则

1. **初赛门优先**：P0.6（提交初赛帖）是参赛资格硬门槛，未提交 = 一切归零。今天必须完成。
2. **保底 > 完美**：阶段一+二完整可用 > 阶段三花哨功能。M14–M15 触发裁剪时整段砍。
3. **每天收工留痕**：5 行进度写入本文件 §6 或计划书，杜绝 4 天信息黑洞。
4. **测试随写随跑**：每完成一个 Rust/TS 模块，`cargo check` / `npx vitest run` 各跑一次。

---

## 1. 立即可执行的安全操作（今天，约 30 分钟）

### 1.1 清理 CRLF 伪改动（消除 git 状态干扰）

> 核实结论：7 个"已修改"文件内容为换行符噪声（字节与 HEAD 完全一致），非真实代码改动。清理后可让真实改动重新可见。

```bash
cd D:/AI工作平台/灵感大王
git checkout -- "deliverables/初赛帖图文/初赛帖.md" \
  src-tauri/migrations/0004_add_favorite_to_prompt_history.sql \
  src/components/ImageExtractResult.tsx \
  src/components/OnboardingOverlay.tsx \
  src/engine/LLMIntentAnalyzer.ts \
  src/engine/SmartFollowup.ts \
  src/question-bank/bank-backend.yaml
git status --short   # 期望：干净或仅剩 ?? 未跟踪项
```

**验收**：`git status --short` 不再显示上述 7 个文件。

### 1.2 提交初赛帖（P0.6）—— 今天必须完成

> 初赛帖草稿 `deliverables/初赛帖图文/初赛帖.md`（31KB，07/05）已完成，仅差"提交到初赛专区"这一步。

**操作清单**：
- [ ] 登录大赛专区：https://forum.trae.cn/c/38-category/40-category/40
- [ ] 发帖，标题建议：`灵感大王 · 桌面悬浮窗提示词优化工具（Trae IDE 开发）`
- [ ] 正文包含：作品介绍 + 功能说明 + 体验链接（双击 HTML / 安装包）+ 图文叙述（6 张截图已备）
- [ ] 上传附件：安装包 `src-tauri/target/release/bundle/nsis/linggandawang_0.1.0_x64-setup.exe` + 创作过程证明材料
- [ ] 确认审核通过，截图留存
- [ ] 记录帖子链接到计划书 §四

**验收**：帖子显示"已发布"，进入初赛作品列表。

### 1.3 补录计划书 v0.2.1（消除 4 天信息差）

需修正计划书以下内容（由主理人执行或你手动改）：
- §一 当前阶段：`M9.5 完成，下一步：A线 初赛帖提交 + B线 M10 项目扫描器` → 改为 `P0.6 待提交 + M10 基础版已入库`
- §四 4.0：P0.5 状态 🔲 → ✅（初赛帖草稿已完成）；P0.6 状态 🔲 → 进行中
- §四 4.1 M10：补充"基础版已提交（lib.rs scan_project 实现文件结构+package.json），待补 T10.3/T10.5/T10.7/T10.8"
- §五 时间线：在 07/04 后补一行 `07/08 ──── 分析复核：M10 基础版已入库，P0.6 待提交，7文件CRLF噪声已澄清`

---

## 2. 14 天冲刺日历

| 日期 | 星期 | 核心任务 | 目标产出 | 截止哨 |
|------|------|----------|----------|--------|
| **07/08** | 三 | P0.6 提交 + CRLF 清理 + 计划书 v0.2.1 | 初赛帖发布、状态干净 | 🔴 初赛门 |
| **07/09** | 四 | M10: T10.3 Git 状态 + T10.7 路径遍历防护 | Rust scan_project 增强 + 单测 | |
| **07/10** | 五 | M10: T10.8 前端仪表盘联调 + T10.5 TODO 扫描(可选) | 扫描结果可展示 | |
| **07/11** | 六 | M10: Rust 单测补全 + `cargo build` 验证 | M10 收尾，测试通过 | |
| **07/12** | 日 | M11: T11.1 核心逻辑 + T9.7 上下文脱敏闭环 | RequirementSyncer 骨架 | |
| **07/13** | 一 | M11: T11.2 系统提示词 + T11.3 子任务解析 | LLM 拆解可用 | |
| **07/14** | 二 | M11: T11.4 同步面板 UI + M12 T12.1 联调 | 阶段二闭环 | ⚠️ 裁剪检查点 |
| **07/15** | 三 | **初赛截止缓冲** + M13 T13.1 阶段二全量测试 | 测试报告 | 🔴 初赛截止 |
| **07/16** | 四 | M13: T13.2 合规 + T13.3 性能 + T13.4 安全渗透 | 阶段二 QA 通过 | |
| **07/17** | 五 | M16: T16.1 视觉一致性 + T16.3 空/错误状态 | 视觉打磨 | |
| **07/18** | 六 | M17: T17.1 演示视频(5min) + T17.2 答辩 PPT | 演示物料 | |
| **07/19** | 日 | M17: T17.3 报名材料终版 + M18 T18.1 Q&A 预演 | 答辩准备 | |
| **07/20** | 一 | M18: T18.2 最终冻结 + 签名打包 | 冻结包 | |
| **07/21** | 二 | 缓冲 / 补测 / 终检 | 待命 | |
| **07/22** | 三 | 🏁 **大赛截止，最终冻结** | 交付 | 🔴 截止 |

---

## 3. 各里程碑可执行拆解

### M10 项目扫描器（基础版已入库，需补全）

**已完成（已提交）**：
- T10.1 Rust 核心：`scan_project` 命令已实现（lib.rs:624）
- T10.2 文件结构：基础版（`list_file_names`/`list_dir_names` 返回 root/src 文件与目录）
- T10.4 依赖图：基础版（`read_package_json` 读取 name/deps）
- T10.8 前端脚手架：`projectScanner.ts`/`ProjectPanel.tsx`/`ProjectBadge.tsx`/`ProjectOnboarding.tsx`/`projectStore.ts` 已存在

**待执行**：

| 任务 | 具体动作 | 文件/命令 | 验收 |
|------|----------|-----------|------|
| T10.3 Git 状态 | 在 `scan_project` 内调用 `git status --porcelain` + `git log -1` 解析分支/改动数 | lib.rs `scan_project` + 新增 `parse_git` 辅助函数 | 扫描结果含 branch/dirty/fileCount |
| T10.7 路径遍历防护 | 校验 `path` 在用户选定根目录内；拒绝 `..`/`symlink` 逃逸；复用 line 268 `SENSITIVE_PATTERNS` 跳过 `.env`/`*.pem`/`*.key` | lib.rs `scan_project` 入口加固 | 尝试 `../../etc/passwd` 被拒 |
| T10.8 前端联调 | `ProjectPanel` 消费 `ScanProjectResult` 展示文件树/技术栈/依赖；加载态+错误态 | `ProjectPanel.tsx` + `projectStore.ts` | 浏览器演示模式 + Tauri 实扫均正常 |
| T10.5 TODO 扫描 | 遍历源码统计 `TODO`/`FIXME`/`HACK` 标记计数 | lib.rs `scan_project` 扩展 | 结果含 todoCount |
| Rust 单测 | 为 `scan_project` 写 3 个用例：正常目录/非目录/路径遍历攻击 | `src-tauri/tests/scan_project_test.rs` | `cargo test` 通过 |

**命令**：
```bash
cd D:/AI工作平台/灵感大王/src-tauri
cargo check          # 改完 Rust 立即类型检查
cargo test           # Rust 单测
```

### M11 需求同步器（未启动）

| 任务 | 具体动作 | 文件 | 验收 |
|------|----------|------|------|
| T9.7 上下文脱敏 | LLM 发送前仅传 `ScanProjectResult` 摘要（文件名/技术栈/依赖），不发源码 | `src/lib/llm/` 新增 `sanitizeProjectContext` | 网络层确认 payload 无源码 |
| T11.1 核心逻辑 | `RequirementSyncer` 接收项目上下文+用户意图 → 调 LLM 拆解需求 | `src/engine/RequirementSyncer.ts`（新增） | 单元测试通过 |
| T11.2 系统提示词 | 需求拆解模板（输出：目标/子任务/阻塞点/验收） | `src/engine/prompts/requirement.ts`（新增） | 结构化 JSON 输出稳定 |
| T11.3 子任务存储 | 解析 LLM 返回 → SQLite `requirement_tasks` 表 | migration 0005 + `src/stores/requirementStore.ts` | 子任务可持久化 |
| T11.4 同步面板 UI | 需求同步面板展示拆解结果 + 手动刷新 | `src/components/RequirementPanel.tsx`（新增） | UI 可交互 |

**命令**：
```bash
cd D:/AI工作平台/灵感大王
npx vitest run src/engine/__tests__/   # TS 单测
npx tsc --noEmit                        # 类型检查
```

### M12–M13 集成与 QA（07/14–07/16）

- T12.1 联调：输入→提问→生成→项目扫描→需求同步 全链路跑通
- T12.2 问题库扩充（P1，可裁）：bank.yaml 30→80（若时间紧跳过）
- T13.1 全量测试：`npx vitest run` 全绿
- T13.2 隐私合规：核对 M8.5 清单（隐私弹窗/keyring/黑名单）全打勾
- T13.3 性能：扫描 <10s、内存 ≤200MB（DevTools 实测）
- T13.4 安全：路径遍历/符号链接/硬编码密钥扫描

### M16–M18 打磨与交付（07/17–07/21）

- T16.1 全局视觉一致性（design token 统一）
- T16.3 空状态/错误状态文案完善
- T17.1 5 分钟演示视频（基于已备 `demo/演示脚本.md`）
- T17.2 答辩 PPT（突出"反向审问"差异化 + 阶段二闭环）
- T17.3 报名材料终版
- T18.1 评委 Q&A 预演（准备 10 个高频问题答案）
- T18.2 最终冻结 + 独立 PowerShell 执行 `npx tauri build` 签名打包

---

## 4. 裁剪预案（触发即执行，不犹豫）

**检查点 07/14（M12 前）**：若 M10/M11 任一未完成 →

1. **先裁**：T10.5 TODO 扫描、T11.6 影响面评估、T12.2 问题库扩充
2. **再裁**：T10.6 Git 监听（改手动刷新按钮）、T11.5 提示词去重
3. **保底**：阶段一完整 + 项目扫描基础版（文件结构+依赖+Git状态） + LLM 接入可用

**检查点 07/17（M16 前）**：若阶段二 QA 未通过 →

- 整段砍掉 **M14–M15**（多轮迭代闭环 / 技术债感知），时间全部压到 M16–M18 保交付。

---

## 5. 每日检查清单

**每天收工前 5 分钟**：
- [ ] `git status --short` 干净或已暂存（无 CRLF 噪声遗留）
- [ ] 当天代码已 `commit`（信息格式：`feat(M10): ...` / `fix: ...`）
- [ ] `npx vitest run` 或 `cargo test` 通过
- [ ] 进度写入本文件 §6 或计划书
- [ ] 明日任务已明确

**每周日（07/12、07/19）**：
- [ ] 全量 `npx tsc --noEmit` + `npx vitest run` + `cargo build` 绿灯
- [ ] 风险看板（§6）更新

---

## 6. 进度追踪与风险看板

### 6.1 每日进度记录

| 日期 | 完成项 | 阻塞 | 次日计划 |
|------|--------|------|----------|
| 07/08 | （待填） | | |
| 07/09 | | | |
| 07/10 | | | |
| ... | | | |

### 6.2 风险看板

| # | 风险 | 等级 | 状态 | 应对 |
|---|------|------|------|------|
| R1 | P0.6 初赛帖未提交 | 🔴 致命 | 待处理（今天） | 见 §1.2 |
| R2 | 阶段二+三范围超时间 | 🔴 高 | 监控 | 07/14 裁剪检查点 |
| R3 | 单人开发 bus factor=1 | 🟠 高 | 持续 | 多智能体借力 + 每日 commit |
| R4 | M10 Rust 无测试 | 🟠 中 | 待处理（07/11） | Rust 单测 3 例 |
| R5 | M9 遗留 T9.4 多模型路由 | 🟡 低 | 可延后 P2 | 不阻塞初赛 |
| R6 | T9.7 脱敏未闭环 | 🟡 中 | 待 M11 | T9.7 并入 M11 |

---

## 7. 可借力的自动化（减少单人瓶颈）

你单人开发的唯一"人力倍增器"是多智能体协作。可让我调度：
- **架构师（高见远）**：在设计 M11 接口时，并行产出 `RequirementSyncer` 的类型定义与 prompt 模板
- **QA（严过关）**：在写 M10 Rust 实现时，并行产出 `scan_project` 的测试用例清单
- **工程师（寇豆码）**：批量实现你确认过的代码模块

**使用方式**：把任务表（T-01~T-10 格式）发我，我按顺序调度团队执行，你只做 build 验证与决策。

---

> 本文档为可执行计划，非状态记录。执行后请在 §6.1 留痕。如需我直接执行 §1 的安全操作（CRLF 清理 + 计划书 v0.2.1 补录），告知即可。
